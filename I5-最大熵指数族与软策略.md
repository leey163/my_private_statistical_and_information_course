# I5｜最大熵、指数族与软策略

> 模块定位：理解为何 softmax、温度、熵 bonus 与 KL 正则会反复在后训练中自然出现。预计 4–5 周。

## 学习内容

- [ ] 最大熵原理：在已知约束下选择最少额外承诺的分布
- [ ] 拉格朗日乘子推导指数族形式
- [ ] softmax / Boltzmann 分布作为期望 reward 约束下的最大熵解
- [ ] 温度、逆温度 β 与探索—利用权衡
- [ ] 熵正则 RL：`E[r] + α H(π)`
- [ ] KL 正则 RL 与 reference policy；与 I3 的闭式解相连
- [ ] log-sum-exp、配分函数与数值稳定性

## 深度验收（L2）

- [ ] 不看资料，用拉格朗日乘子推出有限动作空间中的 Boltzmann policy。
- [ ] 能解释温度趋于 0 和无穷大时策略分别发生什么。
- [ ] 能说明 entropy bonus 与“生成更好”不是同义词：何时提高熵是探索，何时只是噪声。

## 模拟验证练习

```python
import numpy as np
from scipy.special import softmax

rewards = np.array([1.2, 1.0, 0.4, -0.2])
for beta in [0.1, 0.5, 1.0, 2.0, 10.0]:
    pi = softmax(beta * rewards)
    h = -(pi[pi > 0] * np.log(pi[pi > 0])).sum()
    print(beta, pi, 'entropy=', h, 'expected_reward=', pi @ rewards)

# 增加一个 reference policy，比较 exp(reward/beta) 与 ref*exp(reward/beta) 的差异。
```

## 锚定任务

- 主课程 W13：用温度—熵—期望奖励图解释 GRPO/PPO 的探索现象。
- 主课程 W10：解释 PPO 中 KL coefficient 为什么不是普通“超参调大调小”，而是在改变可接受的相对 reference 偏离程度。

## 资料

- MacKay ch31–33（主线）
- Sutton & Barto：maximum-entropy RL 相关段落（连接 RL）
- Haarnoja et al., Soft Actor-Critic（读目标函数，不追理论）

## 不学边界

- 统计物理配分函数的严格理论、连续空间最大熵的测度论细节。
