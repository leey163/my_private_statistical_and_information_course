# I2｜熵、交叉熵与困惑度

> 模块定位：理解训练 loss 曲线实际测量什么，以及“输出更确定”为什么不等于“输出更好”。预计 3–4 周。

## 学习内容

- [ ] 离散熵、联合熵、条件熵；链式法则与条件降低熵
- [ ] 交叉熵 `H(p,q)` 与熵 `H(p)` 的区分
- [ ] `H(p,q)=H(p)+KL(p||q)` 的完整推导
- [ ] perplexity 是平均交叉熵的指数：为何可以比较、何时不可比较
- [ ] 温度缩放如何改变 categorical 分布的熵
- [ ] 熵坍缩、长度效应与 tokenization 对 loss/PPL 比较的影响

## 深度验收（L2）

- [ ] 不看资料推导交叉熵分解，并说明“最小化交叉熵”在哪些条件下等价于匹配真分布。
- [ ] 能解释两模型 PPL 差异为什么不必然等于人类偏好差异。
- [ ] 能从 logits 数值预测调高/调低温度后熵和 top-token 概率的变化方向。

## 模拟验证练习

```python
import numpy as np
from scipy.special import softmax

def entropy(p):
    p = p[p > 0]
    return -(p * np.log(p)).sum()

logits = np.array([3.0, 1.0, 0.0, -1.0])
for temp in [0.2, 0.5, 1.0, 2.0, 5.0]:
    p = softmax(logits / temp)
    print(temp, p, entropy(p))

# 再构造真实 p 与两个 q：一个在高概率 token 上错、一个在低概率 token 上错。
# 比较交叉熵与人眼直觉是否一致。
```

## 锚定任务

- 从主课程任一次 SFT 的 train/eval loss 曲线中选两个 checkpoint：报告交叉熵、PPL、生成多样性（例如 distinct-n）是否同向变化，并解释不一致之处。

## 资料

- MacKay ch2（主线）
- Cover & Thomas ch2（查证）
- StatQuest：Cross Entropy、Perplexity（直觉校准）

## 不学边界

- 连续熵的坐标依赖细节；编码长度的工程实现。
