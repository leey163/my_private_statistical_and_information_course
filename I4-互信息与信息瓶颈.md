# I4｜互信息与信息瓶颈

> 模块定位：用“信息保留/损失”而非单一准确率理解 representation、reward model 与 LLM judge。预计 3–4 周。

## 学习内容

- [ ] 互信息 `I(X;Y)=KL(p(x,y)||p(x)p(y))`
- [ ] 等价表达：熵差、期望 KL
- [ ] 数据处理不等式（DPI）：后处理不能凭空增加关于原变量的信息
- [ ] 条件互信息与交互信息的直觉
- [ ] 信息瓶颈：压缩 X、保留与 Y 有关信息（概念级）
- [ ] judge/RM 作为有损信道：评分压缩了 response 的哪些维度？

## 深度验收（L2）

- [ ] 能用三种等价形式推导互信息，并解释各自在估计时的难点。
- [ ] 能用 DPI 说明：把完整回答压缩为单一 reward 分数后，为什么无法恢复所有质量维度。
- [ ] 能把“judge 与人工不一致”描述为信道噪声与信息损失，而不只说“准确率不足”。

## 模拟验证练习

```python
import numpy as np

# 构造 latent quality -> response features -> judge score 的 toy DAG。
# 添加噪声和量化（连续质量压成 1~5 分），估计 quality 与 score 的 MI。
# 比较：原始 features、单一 score、二元 pass/fail 各保留多少关于 quality 的信息。

# 可先离散化后按频数估计 MI；记录小样本估计偏差。
```

## 锚定任务

- 主课程 W17：将 judge pipeline 画成“人工质量 → 输出 → judge 分数”的信道图；列出可能被压缩掉的质量维度，并为每个维度设计抽样人工审计。

## 资料

- MacKay ch2、ch4（主线）
- Cover & Thomas ch2（DPI 查证）
- Tishby 信息瓶颈论文（只读直觉与目标函数）

## 不学边界

- 神经互信息估计器的理论、部分信息分解（PID）深水区。
