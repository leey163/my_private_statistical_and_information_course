# I1｜概率分布语言精修

> 模块定位：把“模型在生成分布”而不是“模型在输出答案”变成默认思维。预计 3–4 周。

## 学习内容

- [ ] 联合、边缘、条件分布；Bayes 公式与全概率公式
- [ ] 链式法则：`p(x_1:T)=∏_t p(x_t|x_<t)`；理解自回归 LM 是它的直接实例
- [ ] 条件期望、LOTUS、tower property
- [ ] 分布变换与重参数化的直觉
- [ ] 指数族与充分统计量（概念级）：Bernoulli、Categorical、Gaussian、Beta/Dirichlet
- [ ] 共轭先验的用途：不是“信仰贝叶斯”，而是理解分布更新结构

## 深度验收（L2）

- [ ] 能从联合分布推导任意条件概率，并指出“把相关变量边缘化”会丢掉何种信息。
- [ ] 能把一段 token-level NLL 写成条件分布链的负对数，并解释 padding、mask 与长度归一化改变了什么估计对象。
- [ ] 能说明 softmax 输出的 categorical 分布与 logits 的一一对应关系（加常数不变）。

## 模拟验证练习

```python
import numpy as np

# 构造二元联合分布，比较：p(y|x)、p(y) 与错误地假设独立时的偏差。
p_xy = np.array([[0.45, 0.05], [0.10, 0.40]])
p_x = p_xy.sum(axis=1)
p_y = p_xy.sum(axis=0)
p_y_given_x = p_xy / p_x[:, None]

# 构造 toy 自回归二元序列；比较真实链式 likelihood
# 与“token 独立”近似 likelihood 的差异。
```

写下：独立近似在什么条件下可接受？对长文本的依赖结构会造成何种偏差？

## 锚定任务

- 主课程 SFT 实验中，逐项解释 `labels=-100`、attention mask、token 平均 loss、样本平均 loss 分别对应哪个概率对象。

## 资料

- MacKay ch1–3（主线）
- Cover & Thomas ch2（查证）
- Goodfellow et al. *Deep Learning* 概率章节（神经网络语境）

## 不学边界

- Radon–Nikodym 导数、测度论条件期望的严格构造。
